<div class="container mx-auto p-4">
  <h1 class="text-2xl font-bold mb-4">UltrAdex Home</h1>

  <p class="mb-2">Welcome to the UltrAdex application!</p>

  <div class="mb-4 p-3 bg-gray-100 rounded">
    <h2 class="text-xl font-semibold mb-2">Redis Connectivity Test:</h2>
    <% if @redis_error %>
      <p class="text-red-500"><strong>Error:</strong> <%= @redis_error %></p>
    <% end %>
    <p><strong>Message from Redis:</strong> <span id="redis-message" class="font-mono"><%= @redis_message %></span></p>
  </div>

  <div class="mb-4 p-3 bg-blue-100 rounded" data-controller="hello">
    <h2 class="text-xl font-semibold mb-2">Hotwire/Stimulus Test:</h2>
    <p>
      Click the button to update the text below using a Stimulus controller.
    </p>
    <button data-action="click->hello#greet" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded my-2">
      Greet!
    </button>
    <p>
      <strong>Stimulus Output:</strong> <span data-hello-target="output" class="font-mono">Waiting for interaction...</span>
    </p>
  </div>

  <div class="mb-4 p-3 bg-green-100 rounded">
    <h2 class="text-xl font-semibold mb-2">Hotwire/Turbo Frame Test:</h2>
    <%= turbo_frame_tag "turbo_content_area" do %>
      <p>This content is inside a Turbo Frame. Click the link below to update it.</p>
      <%= link_to "Load Other Content", home_turbo_frame_content_path, data: { turbo_frame: "turbo_content_area" }, class: "text-blue-600 hover:underline" %>
    <% end %>
  </div>

</div>

<%# Minimal CSS for demonstration if Tailwind isn't fully effective in test environment %>
<style>
  .font-bold { font-weight: bold; }
  .mb-4 { margin-bottom: 1rem; }
  .p-4 { padding: 1rem; }
  .text-2xl { font-size: 1.5rem; }
  .text-xl { font-size: 1.25rem; }
  .bg-gray-100 { background-color: #f3f4f6; }
  .rounded { border-radius: 0.25rem; }
  .font-mono { font-family: monospace; }
  .text-red-500 { color: #ef4444; }
  .bg-blue-100 { background-color: #dbeafe; }
  .bg-blue-500 { background-color: #3b82f6; }
  .hover\\:bg-blue-700:hover { background-color: #1d4ed8; }
  .text-white { color: #ffffff; }
  .py-2 { padding-top: 0.5rem; padding-bottom: 0.5rem; }
  .px-4 { padding-left: 1rem; padding-right: 1rem; }
  .my-2 { margin-top: 0.5rem; margin-bottom: 0.5rem; }
  .bg-green-100 { background-color: #dcfce7; }
  .text-blue-600 { color: #2563eb; }
  .hover\\:underline:hover { text-decoration: underline; }
</style>
